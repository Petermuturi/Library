<h1>Add new subject</h1>

<%= form_for @subject, :url =>{:action => "create"} do |f| %>
	<%= render 'layouts/errors', object: @subject %>
	<p>Title: <%= f.text_field 'name' %></p>
	<%= f.submit "Save changes" %>
<% end %>

<%= link_to 'Back', {:action => 'list' } %>