<h1>Edit Sbjct Detail</h1>

<%= form_for @subject, :url =>{:action => "update", :id =>@subject} do |f| %>
<%= render 'layouts/errors', object: @subject %>
<p>Name: <%= f.text_field 'name' %></p>
<%= f.submit "Save changes" %>
<% end %>

<%= link_to 'Back', {:action => 'list' } %>